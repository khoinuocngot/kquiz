Third-party browser libraries are loaded lazily from public CDNs by `app.js`.

This folder is reserved for vendored copies if the project later decides to pin local builds for fully offline advanced import/export features.
